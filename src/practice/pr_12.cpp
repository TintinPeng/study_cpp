#include <iostream>

using namespace std;

int main(int argc, char const* argv[])
{
        int i = 0;
        while (i <= 12)
                i = i * 2;
        // x = 0;
        // while (n >= (x + 1) * (x + 1))
        //         x = x + 1;
        return 0;
}